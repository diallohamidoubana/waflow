# Architecture Decision Records (`adr/`)

This directory contains Architecture Decision Records (ADRs) capturing significant architectural choices, rationale, alternatives considered, and consequences for WAFLOW.

Records follow the standard format:

- `NNNN-title.md` (e.g. `0001-monorepo-bootstrap.md`)
