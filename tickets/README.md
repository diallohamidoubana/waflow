# Tickets & Sprints (`tickets/`)

This directory tracks structured implementation tickets and backlog specifications for WAFLOW sprints.

Format:

- `WFL-S{Sprint}-{TicketIndex}.md` (e.g. `WFL-S0-01.md`)
